spec:
  name: mina-eth-balance-signer
  services:
    - environment_slug: node-js
      git:
        branch: main
        repo_clone_url: https://github.com/jackryanservia/mina-eth-balance-signer.git
      name: mina-eth-balance-signer
      http_port: 8080
      instance_count: 1
      instance_size_slug: basic-xs
      name: mina-eth-balance-signer
      routes:
        - path: /
      run_command: npm start
      source_dir: /
